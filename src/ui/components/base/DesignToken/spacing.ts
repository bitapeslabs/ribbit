export const Paddings = {
  small: 14,
  large: 20,
  default: 20,
};

export type PaddingProps = keyof typeof Paddings;

export const Margins = {
  small: 14,
  large: 30,
  default: 20,
};

export type MarginProps = keyof typeof Margins;
